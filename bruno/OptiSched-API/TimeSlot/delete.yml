info:
  name: delete
  type: http
  seq: 16

http:
  method: DELETE
  url: localhost:8080/time-slots/3
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
