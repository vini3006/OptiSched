info:
  name: findByNonExistentId
  type: http
  seq: 14

http:
  method: GET
  url: localhost:8080/time-slots/999
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
