info:
  name: create
  type: http
  seq: 1

http:
  method: POST
  url: localhost:8080/time-slots
  body:
    type: json
    data: |-
      {
          "dayOfWeek": "MONDAY",
          "startTime": "08:00",
          "endTime": "10:00"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
