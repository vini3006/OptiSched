info:
  name: deleteNonExistent
  type: http
  seq: 17

http:
  method: DELETE
  url: localhost:8080/time-slots/999
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
