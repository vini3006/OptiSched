info:
  name: createWithOverlappingEnding
  type: http
  seq: 7

http:
  method: POST
  url: localhost:8080/time-slots
  body:
    type: json
    data: |-
      {
          "dayOfWeek": "FRIDAY",
          "startTime": "09:00",
          "endTime": "11:00"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
