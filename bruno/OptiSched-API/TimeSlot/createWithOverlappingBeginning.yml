info:
  name: createWithOverlappingBeginning
  type: http
  seq: 6

http:
  method: POST
  url: localhost:8080/time-slots
  body:
    type: json
    data: |-
      {
          "dayOfWeek": "TUESDAY",
          "startTime": "07:00",
          "endTime": "09:00"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
