info:
  name: createWithEndEqualsStart
  type: http
  seq: 4

http:
  method: POST
  url: localhost:8080/time-slots
  body:
    type: json
    data: |-
      {
          "dayOfWeek": "MONDAY",
          "startTime": "08:00",
          "endTime": "08:00"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
