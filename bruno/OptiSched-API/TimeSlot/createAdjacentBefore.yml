info:
  name: createAdjacentBefore
  type: http
  seq: 10

http:
  method: POST
  url: localhost:8080/time-slots
  body:
    type: json
    data: |-
      {
          "dayOfWeek": "MONDAY",
          "startTime": "06:00",
          "endTime": "08:00"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
