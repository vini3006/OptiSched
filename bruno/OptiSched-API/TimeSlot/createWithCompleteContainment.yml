info:
  name: createWithCompleteContainment
  type: http
  seq: 8

http:
  method: POST
  url: localhost:8080/time-slots
  body:
    type: json
    data: |-
      {
          "dayOfWeek": "WEDNESDAY",
          "startTime": "08:30",
          "endTime": "09:30"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
