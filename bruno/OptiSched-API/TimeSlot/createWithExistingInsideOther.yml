info:
  name: createWithExistingInsideOther
  type: http
  seq: 9

http:
  method: POST
  url: localhost:8080/time-slots
  body:
    type: json
    data: |-
      {
          "dayOfWeek": "THURSDAY",
          "startTime": "07:00",
          "endTime": "11:00"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
