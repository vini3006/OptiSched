info:
  name: findByDay
  type: http
  seq: 15

http:
  method: GET
  url: localhost:8080/time-slots?dayOfWeek=MONDAY
  params:
    - name: dayOfWeek
      value: MONDAY
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
