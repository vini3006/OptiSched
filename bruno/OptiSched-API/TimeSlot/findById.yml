info:
  name: findById
  type: http
  seq: 13

http:
  method: GET
  url: localhost:8080/time-slots/1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
