info:
  name: update
  type: http
  seq: 7

http:
  method: PUT
  url: localhost:8080/subjects/2
  body:
    type: json
    data: |-
      {
          "name": "Cálculo Diferencial e Integral I",
          "code": "MAT101",
          "workload": 4
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
