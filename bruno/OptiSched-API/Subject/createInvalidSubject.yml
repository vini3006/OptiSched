info:
  name: createInvalidSubject
  type: http
  seq: 2

http:
  method: POST
  url: localhost:8080/subjects
  body:
    type: json
    data: |-
      {
          "name": "",
          "code": "",
          "workload": -1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
