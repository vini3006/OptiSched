info:
  name: updateWithDuplicateCode
  type: http
  seq: 8

http:
  method: PUT
  url: localhost:8080/subjects/3
  body:
    type: json
    data: |-
      {
          "name": "Álgebra Linear",
          "code": "MAT101",
          "workload": 4
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
