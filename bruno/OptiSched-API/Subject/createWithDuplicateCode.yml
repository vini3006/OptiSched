info:
  name: createWithDuplicateCode
  type: http
  seq: 3

http:
  method: POST
  url: localhost:8080/subjects
  body:
    type: json
    data: |-
      {
          "name": "Cálculo 2",
          "code": "MAT101",
          "workload": 4
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
