info:
  name: updateNonExistentSemester
  type: http
  seq: 7

http:
  method: PUT
  url: localhost:8080/semesters/999
  body:
    type: json
    data: |-
      {
          "year": 2026,
          "term": "SECOND"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
