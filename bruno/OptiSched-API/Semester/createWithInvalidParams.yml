info:
  name: createWithInvalidParams
  type: http
  seq: 2

http:
  method: POST
  url: localhost:8080/semesters
  body:
    type: json
    data: |-
      {
          "year": 2026,
          "term": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
