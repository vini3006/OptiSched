info:
  name: update
  type: http
  seq: 4

http:
  method: PUT
  url: localhost:8080/courses/1
  body:
    type: json
    data: |-
      {
          "name": "Engenharia Elétrica"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
