info:
  name: create
  type: http
  seq: 1

http:
  method: POST
  url: localhost:8080/courses
  body:
    type: json
    data: |-
      {
          "name": "Engenharia de Computação"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
