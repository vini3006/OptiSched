info:
  name: updateNonExistent
  type: http
  seq: 7

http:
  method: PUT
  url: localhost:8080/courses/999
  body:
    type: json
    data: |-
      {
          "name": "Engenharia Elétrica"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
