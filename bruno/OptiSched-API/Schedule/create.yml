info:
  name: create
  type: http
  seq: 1

http:
  method: POST
  url: localhost:8080/schedules
  body:
    type: json
    data: |-
      {
          "semesterId": 2
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
