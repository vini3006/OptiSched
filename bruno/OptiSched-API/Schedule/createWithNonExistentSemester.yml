info:
  name: createWithNonExistentSemester
  type: http
  seq: 3

http:
  method: POST
  url: localhost:8080/schedules
  body:
    type: json
    data: |-
      {
          "semesterId": 999
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
