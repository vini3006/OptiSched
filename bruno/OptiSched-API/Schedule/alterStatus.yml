info:
  name: alterStatus
  type: http
  seq: 7

http:
  method: PATCH
  url: localhost:8080/schedules/1/status
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
