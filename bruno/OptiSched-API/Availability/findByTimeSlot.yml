info:
  name: findByTimeSlot
  type: http
  seq: 8

http:
  method: GET
  url: localhost:8080/availabilities?timeSlotId=1
  params:
    - name: timeSlotId
      value: "1"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
