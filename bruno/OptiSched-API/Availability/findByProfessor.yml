info:
  name: findByProfessor
  type: http
  seq: 7

http:
  method: GET
  url: localhost:8080/availabilities?professorId=5
  params:
    - name: professorId
      value: "5"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
