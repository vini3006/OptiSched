info:
  name: createNonExistentTimeSlot
  type: http
  seq: 5

http:
  method: POST
  url: localhost:8080/availabilities
  body:
    type: json
    data: |-
      {
          "professorId": 5,
          "timeSlotId": 999
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
