info:
  name: createWithInvalidParams
  type: http
  seq: 2

http:
  method: POST
  url: localhost:8080/availabilities
  body:
    type: json
    data: |-
      {
          "professorId": null,
          "timeSlotId": null
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
