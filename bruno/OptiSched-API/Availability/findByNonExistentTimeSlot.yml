info:
  name: findByNonExistentTimeSlot
  type: http
  seq: 11

http:
  method: GET
  url: localhost:8080/availabilities?timeSlotId=999
  params:
    - name: timeSlotId
      value: "999"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
