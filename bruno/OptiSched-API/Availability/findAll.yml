info:
  name: findAll
  type: http
  seq: 6

http:
  method: GET
  url: localhost:8080/availabilities
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
