info:
  name: findByProfessorAndTimeSlot
  type: http
  seq: 9

http:
  method: GET
  url: localhost:8080/availabilities?professorId=5&timeSlotId=1
  params:
    - name: professorId
      value: "5"
      type: query
    - name: timeSlotId
      value: "1"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
