info:
  name: findByExistentId
  type: http
  seq: 4

http:
  method: GET
  url: localhost:8080/classrooms/1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
