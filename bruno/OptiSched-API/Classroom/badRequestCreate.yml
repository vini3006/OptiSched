info:
  name: badRequestCreate
  type: http
  seq: 2

http:
  method: POST
  url: localhost:8080/classrooms
  body:
    type: json
    data: |-
      {
          "number": "",
          "capacity": -5
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
