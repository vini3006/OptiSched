info:
  name: update
  type: http
  seq: 6

http:
  method: PUT
  url: localhost:8080/classrooms/1
  body:
    type: json
    data: |-
      {
          "number": "A101",
          "capacity": 45
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
