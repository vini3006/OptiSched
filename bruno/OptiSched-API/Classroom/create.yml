info:
  name: create
  type: http
  seq: 1

http:
  method: POST
  url: localhost:8080/classrooms
  body:
    type: json
    data: |-
      {
          "number": "A102",
          "capacity": 30
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
