info:
  name: createWithDuplicatedEmail
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8080/professors
  body:
    type: json
    data: |-
      {
          "name": "Outro Professor",
          "email": "joao.silva@cefet.br"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
