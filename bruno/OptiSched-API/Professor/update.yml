info:
  name: update
  type: http
  seq: 7

http:
  method: PUT
  url: localhost:8080/professors/2
  body:
    type: json
    data: |-
      {
          "name": "João Silva Atualizado",
          "email": "joao.atualizado@cefet.br"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
