info:
  name: updateWithDuplicateEmail
  type: http
  seq: 11

http:
  method: PUT
  url: localhost:8080/professors/4
  body:
    type: json
    data: |
      {
          "name": "Maria Santos",
          "email": "joao.silva@cefet.br"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
